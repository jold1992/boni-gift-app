---
interface Props {
	src: string;    
    alt: string;
    href: string;
}

const {src, alt, href} = Astro.props;

---
<a class="" href={href}>
    <img class="rounded-full cursor-pointer object-cover" src={src} alt={alt} />
</a>