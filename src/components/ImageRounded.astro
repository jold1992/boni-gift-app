---
interface Props {
	src: string;    
    alt: string;
    href: string;
}

const {src, alt, href} = Astro.props;

---
<a class="" href={href}>
    <img class="rounded-full cursor-pointer" src={src} alt={alt} />
</a>